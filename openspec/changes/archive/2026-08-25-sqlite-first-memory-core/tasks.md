# Tasks: SQLite-first persistent memory core v2

## Authoring contract

The implementation writer loads the installed `tdd` skill before T001 and keeps each test task red before its paired production task. The writer loads `simplify` only after all behavior tasks pass and before the final broad verification. Root alone updates task states from `[ ]` to `[~]` to `[x]` after receiving evidence.

## MVP scope

The first independently testable MVP is US2 + the lexical portion of US3: a clean in-memory/on-disk v2 database can capture evidence, promote/supersede memory, preserve failed history, and immediately retrieve current or historical records through deterministic FTS5 without optional providers. Completion evidence is T001–T010 passing with SC-001–SC-004 demonstrated at the service boundary.

## Dependencies

`T001 -> T002 -> T003 -> T004 -> T005 -> T006`; `T006 -> T007 -> T008 -> T009 -> T010 -> T011 -> T012`; `T012 -> T013 -> T014 -> T015 -> T016`; `T016 -> T017 -> T018 -> T019 -> T020 -> T021 -> T022 -> T023 -> T024`; `T006 -> T025 -> T026 -> T027 -> T028`; `T012 -> T029 -> T030 -> T031 -> T032`; `T024 + T028 + T032 -> T033 -> T034 -> T035 -> T036 -> T037 -> T038 -> T039 -> T040 -> T041 -> T042 -> T043 -> T044 -> T045 -> T046 -> T047 -> T048 -> T049 -> T050 -> T051 -> T052 -> T053 -> T054`.

## Story US2

- [x] T001 [US2] Add failing schema/transaction tests for authoritative evidence, promoted memory, immutable content, temporal outcome, supersession, same-transaction FTS visibility, no-vector offline initialization, optional projection lineage metadata, and v2-only startup covering FR-001, FR-002, FR-003, FR-004, FR-005, FR-006, FR-007, SC-001, and SC-002 in `tests/memory-core/ledger.test.ts` | Verify: tests fail for missing clean tables, evidence links, current/history transitions, immediate FTS recall before commit return, projection lineage, v2 schema marker, and legacy/offline startup boundaries
- [x] T002 [US2] Implement v2 contracts, schema migrations, ledger, and same-transaction FTS maintenance covering FR-001–FR-007 and SC-001–SC-002, with writes restricted to contracts.ts and sqlite/schema.ts, sqlite/migrations.ts, sqlite/ledger.ts, and sqlite/fts.ts in `src/memory-core` | Verify: T001 passes, a legacy database is rejected as non-v2 without mutation, and diff inspection confirms no other memory-core file changed in this task
- [x] T003 [US2] Add failing service tests for evidence-only capture, promotion, failed/mixed outcomes, retraction, topic supersession, and full lineage covering FR-002–FR-003 and SC-001 in `tests/memory-core/service.test.ts` | Verify: tests distinguish current guidance from reachable failed and superseded history
- [x] T004 [US2] Implement the host-neutral MemoryService application boundary and append-oriented memory operations covering FR-001–FR-003 and SC-001 in `src/memory-core/service.ts` | Verify: T003 passes without exposing SQL, host payload, graph, or vector types
- [x] T005 [US2] Add failing project/session identity and lifecycle-receipt idempotency tests covering FR-008, FR-017, FR-027, and SC-004 in `tests/memory-core/identity.test.ts` | Verify: duplicate confirmed events create one logical operation and placeholder identity is never reported as verified
- [x] T006 [US2] Implement the shared identity resolver, session state, and atomic lifecycle receipt behavior covering FR-008 and SC-004 in `src/memory-core/identity.ts` | Verify: T005 passes across restart with stable project/root-session identity

## Story US3

- [x] T007 [US3] Add failing exact/topic/structured/FTS5 tests for code tokens, phrases, prefixes, punctuation-only input, deterministic ties, and immediate post-save recall covering FR-011, FR-012, FR-013, FR-014, FR-015, FR-016, FR-017, SC-002, SC-003, and SC-004 in `tests/memory-core/retrieval.test.ts` | Verify: tests fail without the lexical-first ranked core and safe query builder
- [x] T008 [US3] Implement lexical candidate collection, sanitized query construction, normalized score components, current/history weighting, and deterministic tie breaks covering FR-011–FR-017 and SC-002–SC-004 in `src/memory-core/retrieval` | Verify: T007 passes with zero model, network, vector, graph, or LLM calls
- [x] T009 [US3] Add failing surgical snippet, mem_recall mode=compact→mode=context→mem_get, separate mem_context recovery, project briefing, temporal lineage, explicit compression_ratio, token-accounting, escalation, and compaction-recovery tests covering FR-018, FR-019, FR-020, FR-021, FR-022, FR-023, and SC-003 in `tests/memory-core/context.test.ts` | Verify: tests prove both mem_recall modes, truncation bounds, compression ratio, stable expansion IDs, current/history behavior, and correlated full-fetch credit
- [x] T010 [US3] Implement deterministic surgical trimming, both mem_recall modes, separate mem_context recovery, progressive full fetch, project briefing, and privacy-safe compression/payload/escalation measurements covering FR-018–FR-023 and SC-003 in `src/memory-core/retrieval/context.ts` | Verify: T009 passes, mode=context reports compression_ratio, and every bounded response can escalate by stable ID
- [x] T011 [US3] Add failing optional-projection state, stale watermark, duplicate-job, deletion/rebuild, and lexical-fallback tests covering FR-006, FR-016, FR-024, FR-025, FR-026, FR-027, FR-028, FR-029, FR-030, SC-004, and SC-010 in `tests/memory-core/projections.test.ts` | Verify: tests fail if projection absence changes lexical results or if readiness can be claimed at the wrong watermark
- [x] T012 [US3] Implement projection ports, source-watermark registry, idempotent job contract, optional score normalization, and fail-open fusion covering FR-006, FR-016, FR-024–FR-030 and SC-010 in `src/memory-core/retrieval/projections.ts` | Verify: T011 passes after deleting and rebuilding all projection state from authoritative rows

## Story US5

- [x] T013 [US5] Replace tool contract tests with failing v2 registry, mem_recall compact/context modes, separate mem_context recovery, explicit compression_ratio/bounds, lane-truth, temporal, and removed graph-action cases covering FR-031, FR-032, FR-033, FR-034, FR-035, FR-036, FR-037, FR-038, and SC-003 in `tests/tools/mcp-v2.test.ts` | Verify: the current server fails the exact-six v2 behavior, P4 recall funnel/envelope, and removal of at least one graph/v1 request
- [x] T014 [US5] Rewrite all six handlers against MemoryService and the v2 structured envelope, including required mem_recall compact/context modes and compression_ratio covering FR-031–FR-038 and SC-003 in `src/tools` | Verify: T013 passes and no tool imports the legacy Store, embedding, KG, maintenance, HTTP, or dashboard modules
- [x] T015 [US5] Add failing process/CLI construction tests for one shared v2 core, explicit old-DB refusal, bounded JSON/text output, and no hidden v1 fallback covering FR-007, FR-031–FR-035 and SC-002–SC-003 in `tests/integration-v2.test.ts` | Verify: old entrypoints fail the new construction and compatibility-boundary assertions
- [x] T016 [US5] Wire MCP server, process entrypoint, index exports, and non-import CLI operations to one MemoryService instance covering FR-007 and FR-031–FR-035 in `src` | Verify: T015 passes and a process smoke lists only the six v2 tools against a clean database

## Story US1

- [x] T017 [US1] Add failing lifecycle contract tests for enrollment, recovery, one root prompt, pre/post compact ordering, finalization, retries, restart, and degraded delivery covering FR-039, FR-040, FR-041, FR-042, FR-043, SC-006, and SC-009 in `tests/integration/lifecycle-v2.test.ts` | Verify: tests fail when passive tool/subagent streams create rows or unconfirmed operations advance state
- [x] T018 [US1] Rewrite the integration memory port and lifecycle runtime to call MemoryService with versioned lifecycle intents and receipts covering FR-039–FR-043 and SC-006, SC-009 in `src/integration/core` | Verify: T017 passes with one shared lifecycle implementation and zero direct SQLite access from adapters
- [x] T019 [US1] Add failing versioned capability/payload fixtures for OpenCode, Codex, and Claude Code, including pre-MCP start, compaction, identity failure, and unsupported events covering FR-039, FR-041, FR-042, FR-043, FR-044, FR-045, FR-046, FR-047, FR-048, FR-049, and SC-006 in `tests/integration/adapters-v2.test.ts` | Verify: each host fixture distinguishes execution, memory confirmation, context delivery, and model consumption
- [x] T020 [US1] Rewrite the three adapters and runtime command/output mapping, move parent-chain identity inside OpenCode, and remove the model-callable identity tool covering FR-041–FR-049 and SC-006 in `src/integration` | Verify: T019 passes and no host-specific field reaches MemoryService contracts
- [x] T021 [US1] Add failing canonical-inventory tests requiring hooks, MCP registration, Skills/references, adapter, runner, and manifest ownership for every harness covering FR-039, FR-042, FR-043, FR-050, FR-051, FR-052, SC-005, and SC-007 in `tests/integration/package-v2.test.ts` | Verify: tests name every missing source asset and reject identity-tool or deferred-runtime entries
- [x] T022 [US1] Build the canonical shared/OpenCode/Codex/Claude Code native source bundles and inventory covering FR-039, FR-042–FR-043, FR-050–FR-052 and SC-005 in `integrations` | Verify: T021 passes and every runtime-declared asset resolves once under one harness owner
- [x] T023 [US1] Add failing managed setup/rollback tests for three v2 plugin receipts, coexistence, scope confinement, drift, and unrelated user Skills/config covering FR-043, FR-050–FR-052 and SC-005 in `tests/setup/v2-plugins.test.ts` | Verify: tests reject partial hook/MCP/Skill installation and preserve all unowned user configuration
- [x] T024 [US1] Simplify setup engines/harness planners to install only canonical v2 plugin assets with transactional receipts covering FR-043, FR-050–FR-052 and SC-005 in `src/setup` | Verify: T023 passes for fresh install, idempotent rerun, owned drift, rollback, and forced replacement

## Story US4

- [x] T025 [US4] Add failing frozen-legacy importer tests for path alias rejection, read-only source, supported signatures, deterministic mapping/quarantine, ignored derived state, target integrity, and source hash covering FR-009–FR-010 and SC-011 in `tests/memory-core/importer.test.ts` | Verify: tests fail if the source opens writable, changes bytes, or derived KG/vector rows become authoritative memory
- [x] T026 [US4] Implement the standalone legacy reader, mapping, staging target, validation, and versioned report covering FR-009–FR-010 and SC-011 in `src/memory-core/import` | Verify: T025 passes twice into fresh targets with identical logical dispositions and unchanged source hash
- [x] T027 [US4] Add failing CLI boundary tests for explicit source/target, unknown schema, non-empty target, bounded errors, report path, and cleanup covering FR-009 and SC-011 in `tests/cli/import-v2.test.ts` | Verify: destructive or ambiguous path cases fail before target mutation
- [x] T028 [US4] Expose only the scoped one-way import command and keep the legacy reader out of server imports covering FR-007, FR-009–FR-010 and SC-011 in `src/cli.ts` | Verify: T027 passes and the built MCP/server dependency graph has no path to the legacy reader

## Story US6

- [x] T029 [US6] Add failing report-schema tests for dataset/config hashes, equal budgets, metric namespaces, provenance, resources, unavailable evidence, and promotion reasons covering FR-053, FR-054, FR-055, FR-056, FR-057, FR-058, FR-059, FR-060, SC-008, and SC-012 in `tests/benchmarks/report.test.ts` | Verify: mislabeled Hit@K/Top-K accuracy, unequal token budgets, or missing resource fields reject the report
- [x] T030 [US6] Implement the benchmark manifest, report schema, JSONL provider, resource instrumentation, and fail-closed promotion evaluator covering FR-053–FR-060 and SC-008, SC-012 in `benchmarks` | Verify: T029 passes and the lexical fixture report records zero LLM/network/model bytes plus measured latency/memory
- [x] T031 [US6] Add failing adapter contract fixtures for LongMemEval-S, deterministic LoCoMo, AMB BEAM/PersonaMem sizes, and SDEBench hidden-task outcomes covering FR-053, FR-054, FR-055, FR-056, FR-057, FR-058, FR-059, FR-060, FR-061, FR-062, FR-063, and SC-012 in `tests/benchmarks/adapters.test.ts` | Verify: every required lane either emits its correctly named metrics or an explicit bounded unavailable result
- [x] T032 [US6] Implement thin versioned adapters and one orchestration command with fixed order/budgets and durable reports covering FR-053–FR-063 and SC-012 in `benchmarks` | Verify: T031 passes without fetching data during tests and a fixture decision run is reproducible

## Story US7

- [x] T033 [US7] Add failing packed-artifact and active runtime/package/CI inventory acceptance tests for exact v2 inventory, cold start, hook/MCP/Skill activation, and absence of deferred/model/network dependencies or stale script/workspace/configuration references covering FR-050–FR-052 and SC-005, SC-007 in `tests/packaging/first-product.test.ts` | Verify: the active boundary fails for legacy/deferred assets or startup reachability and remains red until T044 completes manifest closure; test/documentation residue remains assigned to T046–T049 and T051
- [x] T034 [US7] Update source-to-package synchronization and disposable three-host verification to consume only the canonical v2 inventory covering FR-050–FR-052 and SC-005–SC-007 in `scripts` | Verify: script-level synchronization and disposable-host verifier fixtures pass, while T033 intentionally remains red against the current package/runtime boundary
- [x] T035 [US7] Perform the atomic runtime cutover so server, CLI, hooks, and package exports construct only MemoryService covering FR-001, FR-031–FR-035, FR-039–FR-043 and SC-002–SC-007 in `src` | Verify: TypeScript/CodeGraph reference checks find no active runtime entrypoint path to the legacy Store or deferred runtime; legacy test residue remains assigned to T046
- [x] T036 [US7] Delete retired Store/retrieval/indexing/sync/KG/community/maintenance/semantic/HTTP/visualization source after cutover covering FR-004–FR-005, FR-012–FR-014, FR-024–FR-030, FR-036–FR-038 and SC-002, SC-007 in `src` | Verify: build passes and file/import search finds zero retired runtime modules or compatibility fallbacks
- [x] T037 [US7] Remove vector/embedding/HyDE/dashboard/HTTP dependencies and scripts, add v2 benchmark scripts, and narrow first-product files/exports covering FR-004–FR-005, FR-012–FR-014, FR-050–FR-060 and SC-002, SC-005, SC-007–SC-008 in `package.json` | Verify: the manifest declares only first-product runtime dependencies, commands, exports, and packed files; T033 remains red pending the remaining inventory tasks
- [x] T038 [US7] Regenerate dependency resolutions after the manifest reset covering FR-004–FR-005, FR-012–FR-014 and SC-002, SC-007 in `pnpm-lock.yaml` | Verify: no removed model/vector/graph/dashboard importer remains and `pnpm install --frozen-lockfile` succeeds offline from the available store; T033 remains red pending inventory closure
- [x] T039 [US7] Remove dashboard workspace ownership covering FR-052 and SC-007 in `pnpm-workspace.yaml` | Verify: workspace enumeration contains only first-product packages and T033 remains red pending inventory closure
- [x] T040 [US7] Replace HTTP/graph/embedding/semantic/HyDE configuration with the bounded v2 core/plugin schema covering FR-004–FR-005, FR-012–FR-014, FR-039–FR-052 and SC-002, SC-005, SC-007 in `config.schema.json` | Verify: schema fixtures accept v2 core/plugin configuration and reject removed runtime keys; T033 remains red pending inventory closure
- [x] T041 [US7] Remove dashboard/browser/retired-HTTP test inventory while preserving v2 unit, integration, packaging, and benchmark discovery covering FR-050–FR-052 and SC-005, SC-007–SC-008 in `vitest.test-patterns.ts` | Verify: discovered patterns name only retained suites and T033 remains red pending inventory closure
- [x] T042 [US7] Delete the retired browser smoke runner covering FR-052 and SC-007 in `vitest.browser.config.ts` | Verify: active package/runtime configuration no longer references the runner; known workflow and legacy-test references remain assigned to T044 and T046, and T033 remains red
- [x] T043 [US7] Delete the retired browser performance runner covering FR-052 and SC-007 in `vitest.browser-performance.config.ts` | Verify: active package/runtime configuration no longer references the runner; known workflow and legacy-test references remain assigned to T044 and T046, and T033 remains red
- [x] T044 [US7] Remove dashboard/browser/legacy-eval jobs and align CI/release commands with the first-product verification boundary covering FR-050–FR-060 and SC-005, SC-007–SC-008 in `.github/workflows` | Verify: T033 passes against an actual offline packed artifact in isolated homes; frozen-lockfile install, build, and startup succeed without model download; npm pack contains only declared first-product files; and active package/runtime/CI search finds zero runner/workspace/config reference to deleted scripts or surfaces
- [x] T045 [US7] Delete the dashboard/observatory application after the package boundary is clean covering FR-052 and SC-007 in `dashboard` | Verify: active runtime/package/CI file and import search finds zero dashboard, observatory, visualization, or HTTP application residue while package/startup checks remain green; legacy test/documentation residue remains assigned to T046–T049
- [x] T046 [US7] Remove v1-only tests/fixtures and retain or rewrite every behavior contract required by v2 covering FR-001–FR-063 and SC-001–SC-012 in `tests` | Verify: test discovery contains no assertion that requires a removed v1 graph/vector/HTTP/dashboard/compatibility behavior
- [x] T047 [US7] Update the product README to the clean v2 architecture, six-tool semantics, three plugins, importer, benchmark caveats, and removed surfaces covering FR-031–FR-063 and SC-005, SC-007, SC-011–SC-012 in `README.md` | Verify: public positioning and commands match the packed first product and make no required/default vector, KG, HTTP, or dashboard claim
- [x] T048 [US7] Update routed agent/operator documentation to the v2 architecture, verification commands, data boundary, importer, and benchmark semantics covering FR-031–FR-063 and SC-005, SC-007, SC-011–SC-012 in `docs/agent` | Verify: documented commands exist in package manifests and no routed guide instructs agents to use a removed runtime surface
- [x] T049 [US7] Update the root repository guide and map to the v2 first-product boundary covering FR-031–FR-052 and SC-005, SC-007 in `AGENTS.md` | Verify: the active root guide names only retained source, runtime, test, package, and documentation surfaces

## Parallel execution

- None: the plan deliberately assigns one product writer because schema, MemoryService, six tools, lifecycle adapters, package inventory, deletion, and tests share contracts and entrypoints; apparent file-level parallelism would violate the one-writer cutover and raise integration risk.

## Final verification

SC-013 and SC-014 remain outcome gates: they are measured by a full decision benchmark after the buildable harness exists and do not justify adding a speculative implementation. A candidate remains lexical-only/default-off until those reports pass.

- [x] T050 Simplify only changed v2 production/test surfaces without changing behavior after all story tasks pass covering FR-001–FR-063 in `src/memory-core` | Verify: focused suites remain green and diff review shows no compatibility shim or optional feature promoted accidentally
- [x] T051 Run the repository-required focused suites, frozen-lockfile install, TypeScript build, full Vitest suite, package prepublish checks, and repository-wide deleted-reference audit covering SC-001–SC-012 in `package.json` | Verify: every executed command exits successfully, zero runtime/test/workflow/workspace/config/documentation reference to deleted surfaces remains, or the exact failure is recorded before Oracle review
- [x] T052 Run packed-artifact smoke from the actual tarball in disposable OpenCode, Codex, and Claude Code homes covering SC-005–SC-007 in `scripts/verify-integration-package.mjs` | Verify: all three plugins prove asset activation and truthful lifecycle behavior without touching real user host state
- [x] T053 Run the committed fixture benchmark plus any available full decision lanes and record SC-008, SC-012, SC-013, and SC-014 status covering FR-053–FR-063 in `benchmarks/run.mjs` | Verify: report schema passes, unavailable lanes are explicit, and no optional default changes without a passing promotion result
- [x] T054 Delegate final read-only verification to a fresh Oracle and persist its independent PASS/FAIL evidence covering FR-001–FR-063 and SC-001–SC-014 in `openspec/changes/sqlite-first-memory-core/verify-report.md` | Verify: PASS contains requirement/task/check/diff/package/migration evidence, or FAIL returns actionable defects to the convergence loop

## Convergence after Oracle verification 1

- [x] T055 [US3] Remediate O1-F001 (classification: contradicts) by enforcing aggregate context budgets, checkpoint and handoff recovery, accurate source/evidence/returned/full measurements, and correlated full-fetch escalation in `src/memory-core` | Verify: the Oracle reproductions stay within the requested budget, recover source-attributed evidence, and expose same-correlation escalation telemetry
- [x] T056 [US3] Remediate O1-F002 (classification: missing) with stable projection source mappings, configuration and source mismatch handling, retryable jobs and checkpoints, rebuild from authoritative SQLite, and authoritative-hash equivalence in `src/memory-core/retrieval/projections.ts` | Verify: interrupted jobs converge and delete/rebuild reaches the same watermark and projection hash without changing authoritative hashes
- [x] T057 [US1] Remediate O1-F003 (classification: contradicts) by repairing native payload translation, recovery and capability truth, installed-bundle path resolution, copied-plugin full lifecycle smoke, and stale owner removal in `integrations` | Verify: native-shaped fixtures execute installed copied bundles for all three hosts through enrollment, prompt, compact, recover, finalize, retry, degraded, and unsupported paths
- [x] T058 [US4] Remediate O1-F004 (classification: partial) with deterministic versioned importer success and failure reports, schema versions, timestamps, disposition classes, bounded reasons and codes, and per-type counts in `src/memory-core/import/legacy-v1.ts` | Verify: successful and failed fresh-target runs emit schema-valid reports with identical dispositions and byte-identical sources
- [x] T059 [US6] Remediate O1-F005 (classification: contradicts) with full same-condition validation, multi-sample resources, progressive escalation and compaction fields, and fail-closed quality/resource gates in `benchmarks` | Verify: incomparable or resource and quality-regressing candidates cannot promote and the real fixture report retains explicit unavailable external lanes

## Convergence after Oracle verification 2

- [x] T060 [US3] Remediate O2-F001 (classification: partial) by completing the mem_context measurement, source, correlation, and escalation envelope in `src/tools/index.ts` | Verify: context reports source, evidence, returned, full, token-basis, stable evidence IDs, correlation ID, and a same-correlation mem_get escalation without exceeding the caller budget
- [x] T061 [US6] Remediate O2-F002 (classification: contradicts) by rejecting every declared retrieval, answer, and agent primary-metric regression and adding fallback-state, operational-error, and source/evidence payload evidence in `benchmarks` | Verify: a comparable MRR gain with answer or hidden-test regression cannot promote and reports cover disabled, missing, stale, rebuilding, failed, and source-mismatched projection controls
- [x] T062 [US7] Remediate O2-F003 (classification: contradicts) by making every convergence task validator-compliant and sequential in `openspec/changes/sqlite-first-memory-core/tasks.md` | Verify: the Full ready validator exits successfully after T055-T062 are present

## Convergence after Oracle verification 3

- [x] T063 [US3] Remediate O3-F001 (classification: contradicts) by reconciling correlation-level progressive telemetry after full-fetch escalation in `src/memory-core/service.ts` | Verify: a context-only answer receives avoided credit only when finalized, while a same-correlation mem_get path ends with full_fetches one and avoided_full_fetches zero
- [x] T064 [US7] Remediate O3-F002 (classification: contradicts) by deleting the obsolete tracked plugin mirror and its retired registrations in `plugin` | Verify: tracked/runtime/package/reference audits find no plugin mirror or deleted hook, MCP, Skill, graph, or no-http contract
- [x] T065 [US7] Complete O3-F002 (classification: contradicts) by deleting the obsolete tracked v1 Skill mirror and its eval/reference assets in `skills/thoth-mem` | Verify: repository-wide guidance contains no retired graph action, old lifecycle parameters, semantic fallback, or 30-second deduplication contract outside canonical integrations

## Convergence after Oracle verification 4

- [x] T066 [US3] Remediate O4-F001 (classification: contradicts) by atomically finalizing projection job and registry readiness and healing ready-job/non-ready-registry restarts in `src/memory-core/retrieval/projections.ts` | Verify: an on-disk crash-window fixture retries to ready with matching job/registry watermarks and hashes, effective state ready, duplicate convergence, and unchanged authoritative hashes
