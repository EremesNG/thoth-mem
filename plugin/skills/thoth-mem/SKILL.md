---
name: thoth-mem
description: Use SQLite-first project memory through the six v2 MCP tools.
---
Use `mem_recall` compact, then context, then `mem_get`. Save only durable evidence; never store private blocks. Load the reference for the active host when lifecycle identity matters.
